<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="vangiau.example.ecobicyclerent.activity" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon_app"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EcoBicycleRent" >
        <activity android:name=".ThueXeThanhCongActivity" />
        <activity android:name=".ThueXeActivity" />
        <activity android:name=".PaymentDetails" />
        <activity android:name=".NapTienVaoTheActivity" />
        <activity android:name=".DangKyThanhCongActivity" />
        <activity android:name=".TheTraSauActivity" />
        <activity android:name=".TheTraTruocActivity" />
        <activity android:name=".DangKyMuaTheActivity" />
        <activity android:name=".TimKiemActivity" />
        <activity android:name=".MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>